import './sendList.scss';

// 发送列表
export default () => {
  return (
    <div data-class='sendlist'>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
      <div style={{height: '80px'}}>send</div>
    </div>
  );
};
